<%= form_for @song do |f| %>
  <% if @song.errors.any? %>
    <div id="errors">
      <h2><%= pluralize(@song.errors.count, "error") %> stopped this song from saving</h2>
      <ul>
        <%= @song.errors.full_messages.each do |msg| %>
          <li> <%= msg %> </li>
        <% end %>
      </ul>
    </div>

  <% end %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :attachment %><br>
    <%= f.text_area :attachment %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>
